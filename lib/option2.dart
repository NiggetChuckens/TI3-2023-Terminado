import 'package:flutter/material.dart';

class Option2Page extends StatelessWidget {
  const Option2Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Option 2'),
      ),
      body: const Center(
        child: Text('Option 2 Page'),
      ),
    );
  }
}
